[package]
name = "falcon-agent-capture"
version = "1.0.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
falcon-sdk = { path = "../../../crates/sdk" }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.52", features = [
    "Win32_Foundation",
    "Win32_Graphics_Dwm"
] }
